# Commands Directory

This directory contains custom slash commands for Claude Code.

## What are Commands?

Commands are shortcuts that trigger specific workflows or operations. They are invoked using the slash notation (e.g., `/command-name`).

## Structure

Each command file should:

- Define clear command syntax
- Document expected inputs
- Specify command behavior
- Include usage examples

## Creating Commands

Commands should be concise and focused on automating common tasks or workflows.

## Available Commands

(Commands will be added as they are developed)
